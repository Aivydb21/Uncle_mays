# Pinned to the versions the ingest scripts were written against.
# Install with:  python -m pip install -r ml/requirements.txt
polars>=1.20,<2
pyarrow>=18.0
httpx>=0.27
python-dotenv>=1.0
google-auth>=2.35
google-auth-httplib2>=0.2
google-api-python-client>=2.150
duckdb>=1.1
tzdata>=2024.2  # Python 3.14 on Windows can't find UTC tzdata without this
# Modeling deps (added when we move past dataset-building):
# scikit-learn>=1.5
# xgboost>=2.1
# mlflow>=2.16
# jupyter>=1.1
